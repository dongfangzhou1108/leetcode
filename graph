图：
并查集：
  确认是否有连接关系
  数组 索引：元素 值：父节点
  find:寻找某个索引的父节点 union：链接两个节点，更新相应父节点
  模式一：quick——find 
    在union的时候，两者find结果不一致，需要便利所有节点，选择一个结果进行更正
  模式二：quick-union
    在union的时候，两者find结果不一样，只更改其中一个节点，在find的时候稍微浪费时间，必须满足数组索引和值一样
